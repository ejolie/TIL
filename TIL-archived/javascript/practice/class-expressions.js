let Polygon = class { // class Polygon can be too.
    constructor(height, width) {
        this.height = height;
        this.width = width;
    }
}
console.log('Polygon:', Polygon);
let p = new Polygon(1, 2);
console.log('p:', p);